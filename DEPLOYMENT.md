# Deployment Notes

Set these environment variables in Vercel before deploying:

- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`

The production site URL used for SEO, robots, sitemap, and Open Graph metadata is `https://uygulamapazar.com`.
